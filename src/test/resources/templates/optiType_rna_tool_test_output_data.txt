echo `date` Begin Step: OptiType HLA typing...


 optiType/OptiTypePipeline.py -i merged_fastq1.gz merged_fastq2.gz
--rna --config optiType/config.ini --outdir output/optiType
mv output/optiType/hla_result.tsv output/optiType/sampleName.mhc1hla.type.results
mv output/optiType/hla_coverage_plot.pdf output/optiType/sampleName.mhc1hla.coverage
if [ $? -eq 0 ]
then
	echo `date` Successful Step: OptiType HLA typing.
	sleep 8
else
	echo `date` Error Step: OptiType HLA typing.

	echo `date` The job was aborted due to ERRORS found.
	exit 1;
fi